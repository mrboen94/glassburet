export default function EventCard({ children }) {
  return (
    <div className="mx-auto max-w-md p-2 m-2 shadow-md rounded-lg hover:shadow-lg transition-all duration-500 bg-green-200">
      {console.log(children)}
    </div>
  );
}

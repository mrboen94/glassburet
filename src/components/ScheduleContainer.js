export default function ScheduleContainer({ children }) {
  return (
    <div className="mx-auto max-w-md bg-red-200">
      Hello bby
      <p>imagine using html tags</p>
      {children}
    </div>
  );
}
